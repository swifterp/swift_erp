<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="resources.mapper.insertListMapper">
	<insert id="empAdd" parameterType="HashMap">		
        insert into employee(emp_number,emp_name,emp_email) values (#{emp_number, jdbcType=VARCHAR},#{emp_name, jdbcType=VARCHAR}, #{emp_email, jdbcType=VARCHAR})
	</insert>
</mapper>