<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="resources.mapper.selectListMapper">

	<select id="getAttendCodeList" resultType="hashmap">
	
		select attend_code, attend_name, attend_code_state, attend_c_etc from ATTEND_CODE_T
	
	</select>
	
	<select id="getHolidayCodeList" resultType="hashmap">
	
		select holiday_name, holiday_date_start, holiday_code_state, attend_code, holiday_date_end from HOLIDAY_CODE_T
	
	</select>
	
	<select id="getAttendManagerList" resultType="hashmap">
	
		select to_char(attend_date,'YYYY-MM-DD') as attend_date, empno, attend_time, attend_m_etc, attend_code, attend_no from ATTEND_MANAGER_T
	
	</select>
	
	<select id="getAttendAddList" resultType="hashmap">
	
		select attend_code, attend_name, attend_code_state, attend_c_etc from ATTEND_CODE_T
	
	</select>

</mapper>